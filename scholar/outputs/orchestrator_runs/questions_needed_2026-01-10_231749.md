# Questions Needed - 2026-01-10_231749

Q: Should audits use a fallback to the most recent prior session when the 7-day window is empty, or use strict empty-window reporting only?
A: I don't have an opinion on this.
Q: Should the session log template require a "Pre-probe attempted? Y/N + evidence" field to enforce probe-before-teach documentation?
A: (pending)
Q: Should technique checklist coverage be explicitly captured in session logs (checkboxes) instead of inferred from reflections?
A: (pending)
